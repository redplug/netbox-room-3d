# v0.1.5 성능 개선

측정일: 2026-09-10. 운영 서버가 아닌 macOS의 로컬 Chromium 153에서 합성 데이터를 사용했습니다.

## 변경 사항

- 방, 랙, 룸 오브젝트를 안정적인 ID별 그룹으로 유지합니다. 장비·치수·표시 옵션 변경 시 해당 랙만 다시 만듭니다.
- 3D/평면 전환 시 상세 장비 그룹과 간단한 평면 랙 그룹을 교체 연결합니다. 상세 메시와 텍스처는 재사용합니다.
- 드래그는 프레임당 최대 한 번 좌표만 갱신합니다. 종료 시 마지막 입력, 충돌 검사, 저장 상태, undo를 확정합니다.
- pointercancel, 포커스 이탈, 포인터 캡처 해제, Esc는 진행 중인 이동을 취소합니다.
- 선택 강조와 상태 필터는 랙을 재생성하지 않습니다. UI만 바뀌는 보기·검색 처리에서 불필요한 장면 갱신을 줄였습니다.
- 같은 글자·포트·이미지는 참조 횟수 기반 캐시를 공유합니다. 미사용 자원은 최대 128개만 보관하며, 장면 종료 시 전부 해제합니다.
- 포트 텍스처를 512×512에서 128×128로 줄였습니다. 포트의 화면 내 물리 크기와 Primary IP 표시 규칙은 유지합니다.

## 동일 조건 CPU 비교

랙 24개, 장비 480개, 인터페이스 1,920개. 화면 1440×900, 측정 장면 1280×800, deviceScaleFactor 1.
전환 6회, 좌표 이동 12회. 리팩토링 직전 작업트리(화면 높이/앞뒤 표기 변경 포함)와 개선 코드를 비교했습니다.

| 항목 | 개선 전 | 개선 후 |
|---|---:|---:|
| 최초 장면 준비 | 302.1ms | 41.9ms |
| 보기 전환 중앙값 | 400.1ms | 0.9ms |
| 좌표 이동 중앙값 | 402.4ms | 타이머 해상도 미만 (<0.1ms) |
| 전환 6회 중 전체 clear / 글자 생성 | 6 / 32,544 | 0 / 0 |
| 이동 12회 중 전체 clear / 글자 생성 | 12 / 65,088 | 0 / 0 |

원자료: [개선 전](benchmarks/baseline-cpu.json), [개선 후](benchmarks/optimized-cpu.json).
수치는 JavaScript 장면 준비 경로만 측정하며 WebGL 렌더링과 CSS 라벨 렌더링은 제외합니다.
0ms 표본은 실제 비용이 없다는 뜻이 아니라 브라우저 타이머 분해능 미만이라는 뜻입니다.
FPS, 사용자 체감 지연, 운영 환경 성능을 뜻하지 않으며 이미지 다운로드·API 지연도 포함하지 않습니다.
별도 브라우저 테스트는 실제 렌더러, 마우스 드래그, 모드 전환, 리소스 수, 저장/복원을 검증합니다.

## 재현

```sh
npm ci
npm run dev
# 다른 터미널에서 (Chromium 설치 필요: npx playwright install chromium)
node scripts/benchmark.mjs optimized-cpu 24
npm test
npm run test:e2e
```

현재 코드 실행 결과는 `artifacts/performance-optimized-cpu.json`에 기록됩니다.
`baseline`은 출력 파일 라벨일 뿐, 이전 코드로 자동 전환하는 옵션이 아닙니다.
과거 baseline 결과는 위 원자료에 보관했습니다. 전체 E2E에는 로컬 NetBox 18080도 필요합니다.

## 남은 비용과 제약

첫 로딩과 장비/표시 옵션 변경에는 메시 생성 비용이 남습니다. 3D의 상세 메시 수에 따른 GPU 비용도 남아 있습니다.
대규모 3D 장면에서 지속 FPS 문제가 있으면 인스턴싱/LOD 및 실제 운영 데이터 기반 프로파일링을 별도로 검토해야 합니다.
이번 릴리스는 DB 스키마·저장 JSON·권한을 변경하지 않습니다. 운영 서버 적용은 [UPGRADE.md](UPGRADE.md)를 따르세요.
