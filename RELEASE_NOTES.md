# v0.1.5 — 3D 전환·평면 드래그 성능 개선

- 랙별 객체 재사용, 모드별 그룹 연결, 드래그 좌표 전용 갱신 및 프레임 단위 처리.
- 글자·이미지 텍스처 공유 및 미사용 자원 캐시 제한, 종료 시 리소스 해제.
- 드래그 취소/포커스 이탈 복원과 한 번의 이동에 한 번의 undo 처리.
- NetBox 화면 높이 자동 맞춤, 평면 랙 전·후면 표기 및 회전 연동.
- 합성 랙 24개/장비 480개 기준 전환 CPU 중앙값 400.1ms → 0.9ms. GPU 렌더링 제외.
- DB 마이그레이션 및 저장 JSON 변경 없음. 기존 레이아웃 유지.

[측정 방법과 제한](https://github.com/redplug/netbox-room-3d/blob/v0.1.5/PERFORMANCE.md) · [데이터 유지 업데이트](https://github.com/redplug/netbox-room-3d/blob/v0.1.5/UPGRADE.md)

운영 서버에는 적용하지 않았습니다. wheel/sdist는 첨부 SHA256SUMS로 확인하세요.

---

# v0.1.4 — 검색·위치 강조 및 랙 운영 표시

- 서버명·IP 부분 검색, 좌측 결과에 일치 IP 표시.
- 랙·서버 선택 시 카메라 이동과 5초간 노란 맥동 강조, 이후 고정 표시. 동작 줄이기 설정 지원.
- 전·후면 U 번호·빈 슬롯 안내선, 상태 배지·필터.
- 랙별 사용/잔여 U, 장비 수, 사용률 표시. 조회 가능한 장비만 집계.
- IP 조회 권한 적용. DB 마이그레이션 및 배치 JSON 형식 변경 없음.

운영 서버에는 적용하지 않았습니다. [업데이트 방법](https://github.com/redplug/netbox-room-3d/blob/v0.1.4/UPGRADE.md)

---

# v0.1.3 — 룸 오브젝트 및 표시 기능

- 랙 전·후면 표지, 장비 전면 서버명, 후면 인터페이스 이름 표시.
- 서버 상·하단 기본 회색 및 전역 할당 색상 토글.
- 인터페이스는 최대 0.5U 정사각형 포트로 표시하고 Primary IP 할당 포트는 노란 테두리로 강조.
- Primary IP 1개는 서버 전체 마우스 오버, 2개 이상은 해당 인터페이스 마우스 오버 시 연결된 IP만 표시. 인터페이스·IP 객체 조회 권한 적용.

- 장비 이미지를 선택 여부와 관계없이 모든 랙에 표시 (3D/워킹).
- 랙 좌우 측면을 닫거나 여는 표시 토글.
- UPS, 항온항습기, 배터리 캐비닛, 책상, 문, 유리벽, 벽, 사용 불가 공간 추가.
- 룸 오브젝트 위치·크기·방향 편집, 평면 드래그, 저장·복원.
- 랙이 직접 소속된 Location만 표시하는 체크박스. 조회 권한 적용 및 현재 편집 유지.

DB 마이그레이션 없음. 기존 레이아웃 유지. 새 오브젝트 JSON에는 종류와 회전값을 저장합니다.
프런트엔드와 서버 코드를 함께 업데이트하세요. 0.1.2로 롤백한 뒤 새 오브젝트를 재저장하면 종류·방향이 손실될 수 있습니다.
[데이터 유지 업데이트 및 롤백 가이드](https://github.com/redplug/netbox-room-3d/blob/v0.1.3/UPGRADE.md)

문은 닫힌 형태이며 개폐 기능은 없습니다. 운영 서버에는 적용하지 않았습니다.

---

# v0.1.2 — 워킹 모드

- 서버실 눈높이에서 WASD/방향키 이동, 드래그 시점 회전, Shift 빠른 이동, Esc 종료.
- 랙·장애물·서버실 경계 충돌 방지, 포커스 이탈 시 이동 중단, 시작 위치 복귀.
- 기존 배치·장비 상세 조회와 연동. 워킹 시점은 DB에 저장하지 않습니다.
- 기존 설치를 위한 [데이터 유지 업데이트 가이드](https://github.com/redplug/netbox-room-3d/blob/v0.1.2/UPGRADE.md): Linux/systemd, Docker, 백업·검증·롤백.

v0.1.1 대비 DB 모델, 마이그레이션, 저장 API와 JSON 형식 변경 없음.
기존 DB·미디어·설정을 유지하고 플러그인 패키지와 정적 파일만 교체합니다.
NetBox 4.5.x / Python 3.12 이상. 운영 서버에는 적용하지 않았습니다.

wheel과 소스 압축파일은 첨부 SHA256SUMS로 확인하세요.

---

# v0.1.1

Location 기반 서버실 공간을 설정하고 기존 NetBox 랙을 수동 배치하는 3D 뷰어입니다.

- 서버실 크기·격자·장애물 블록, 랙 이동·회전·잠금·충돌 검사.
- U 위치 기반 장비 표시, 전면·후면 이미지 및 장비별 색상.
- 권한 검사, 레이아웃 저장·복원, 동시 편집 충돌 방지.
- 상단 제목 영역 제거. 서버실 설정 줄에 저장 상태·취소·배치 저장 통합.
- README에 Linux/systemd 및 기존 netbox-docker 배포, 권한, 사용법, 업데이트·롤백 안내.

패키지 지원 범위는 NetBox 4.5.x이며, 실제 통합 검증 버전은 4.5.0입니다.
브라우저 통합 시나리오 3개가 통과했습니다. 서버/계산 검증 내역은 VERIFICATION.md를 참고하세요.

설치 시 wheel을 사용하면 운영 서버에 Node.js가 필요하지 않습니다.
첨부된 SHA256SUMS로 파일 무결성을 확인하세요.
운영 서버에 직접 설치하거나 운영 인벤토리를 변경한 릴리스는 아닙니다.
