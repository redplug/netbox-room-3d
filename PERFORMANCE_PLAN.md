# v0.1.5 성능 개선 계획

## 목표와 범위

3D/평면 전환과 평면 랙·오브젝트 드래그의 불필요한 전체 장면 재생성을 제거한다.
기존 데이터, 권한, 저장/되돌리기, 충돌 검증, 워킹, 검색 강조, 이미지/IP, 표시 옵션을 유지한다.
이미 작업 중인 화면 높이 자동 조정과 평면 전·후면 표기도 함께 보존하여 배포한다.
운영 서버에는 직접 설치하지 않고 테스트 서버 검증 후 GitHub 소스 및 설치 릴리스를 게시한다.

## 확인된 병목

- `changed → render → scene.update → clear`가 포인터 이동마다 전체 메시·글자를 다시 생성한다.
- 보기 버튼 처리와 공통 finally에서 render가 중복 호출된다.
- 보기 모드만 바뀌어도 재사용 가능한 장비·텍스처·U 번호를 다시 생성한다.
- 동일한 문자·포트 이름의 CanvasTexture와 재질을 공유하지 않는다.
- 선택/검색/저장 버튼 상태 변경도 3D 재생성을 유발한다.

## 구현 순서

1. 현재 코드의 합성 데이터 벤치마크를 기록하고 재현 가능한 측정 스크립트를 만든다.
2. 장면을 방/랙/오브젝트/선택 오버레이로 분리하고 안정적인 ID로 참조한다.
   랙 내용·치수 변경만 해당 그룹을 재생성하고 좌표 변경은 transform만 갱신한다.
3. 보기 전환은 기존 그룹과 카메라·모드별 표시만 바꾼다. 평면에서 불필요한 세부 렌더링을 생략하되 3D 데이터는 보존한다.
4. 드래그는 프레임당 최대 한 번의 좌표 갱신과 그리기를 수행한다. 종료 시 최종 좌표를 반영하고 UI·충돌·dirty·undo를 확정한다.
   pointercancel/포커스 이탈도 편집 상태가 남거나 저장 검사가 빠지지 않도록 처리한다.
5. 텍스트/이미지 재질과 텍스처를 재사용하고 참조가 없어진 자원은 제한된 캐시/해제로 관리한다.
6. 클릭 처리의 중복 render를 없애고 UI 갱신과 장면 갱신 책임을 분리한다.

## 검증 기준

- 동일 데이터에서 드래그/보기 전환 중 기존 랙 그룹·장비 메시·텍스처 ID가 유지된다.
- 포인터 이동 N회가 전체 재생성 N회로 이어지지 않는다. 드래그 종료 좌표는 마지막 입력과 일치한다.
- 잠금, 읽기 전용, 충돌/경계 초과 저장 차단, undo/cancel/reload, 회전, 오브젝트 이동이 유지된다.
- 보기 전환으로 평면 앞뒤 표기, U/상태/사용량, 전후 이미지, IP 툴팁이 사라지거나 잘못 남지 않는다.
- 반복 전환·삭제·Location 교체 시 자원 수가 무한 증가하지 않는다.
- 같은 브라우저·합성 데이터에서 변경 전/후 CPU 처리 시간과 장면 생성 횟수를 비교한다.
  수치는 운영 서버 실측으로 주장하지 않는다. 목표는 정상 드래그와 보기 전환의 전체 재생성 0회이다.
- 단위/Django/브라우저 회귀 테스트, 다양한 해상도 및 큰 합성 장면을 확인한다.
- 최종 문서: 이 계획, PERFORMANCE.md(측정/제약), README/UPGRADE/릴리스 노트.
- 최종 배포: v0.1.5 소스 커밋·푸시, wheel/sdist/SHA256SUMS 릴리스 및 원격 검증.
